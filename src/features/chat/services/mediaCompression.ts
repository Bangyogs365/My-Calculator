export async function compressMedia(file: File) {
  // Production hook:
  // image -> webp compression
  // video -> optimized mp4
  // audio -> optimized voice format
  // Implementation can use client compression library.

  return file;
}
